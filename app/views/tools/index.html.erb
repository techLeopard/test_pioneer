<% provide(:title, 'Tools') %>

<%= link_to 'Back to Interview', interview_path %> |
<%= link_to 'Добавить инструмент', new_tool_path %>

<h1>Инструменты</h1>

<div>
  <ul class='contents-list'>
    <% @tools.each do |tool| %>
      <li><%= link_to tool.title, tool_path(tool) %></li>
    <% end %>
  </ul>
</div>