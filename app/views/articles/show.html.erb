<% provide(:title, @article.title)%>
<div class='article-controls'>
  <p>
    <strong>Раздел:</strong> <%= @article.section.title %>
  </p>
  <%= link_to '[back]', section_articles_path %> |
  <%= link_to '[edit]', edit_section_article_path(@article.section, @article) %> |
  <%= link_to '[delete]', section_article_path(@section, @article), method: :delete, data: { confirm: 'Вы уверены?' } %>
</div>
<h1><%= @article.title %></h1>

<div class='article-text'>
  <p><%= @article.content.html_safe %></p>
</div>
